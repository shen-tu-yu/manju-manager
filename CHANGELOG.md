# 更新日志

> 面向**使用者**：这里只写"能感觉到什么变了"。
> 代码结构、踩坑、排查规则在 [`CODE_MAP.md`](CODE_MAP.md)。

## 2026-09-26

### ✨ 弹窗都能拖、能缩放了（位置大小还会记住）

- **所有弹窗**（提示词工作台、📄 原文窗口、⚙ 预设、设置、分镜预览、新文件入库卡片）现在统一支持：
  - **按住标题栏拖动**换位置（标题栏光标变成"可移动"，鼠标停上去有提示）
  - **拖右下角**改大小（右下角有两道斜线，看到就知道能拉）
  - **双击标题栏** = 最大化 / 再双击还原
  - **位置和大小自动记住**：关掉、刷新、下次打开还在原地（工作台的"大窗 / 小窗"各记一套）
- 细节都处理了：
  - 拖出屏幕也能抓回来（**至少留 80px 在视口里**）；浏览器窗口变小时浮层自动夹回
  - 尺寸有下限，不会缩到看不见；也不会缩得比屏幕还大
  - 右下角的按钮（"入库""保存"这类）不会被误当成缩放手柄
  - 模态框高度默认**跟着内容自适应**，你手动拉过之后才固定
- 顺带修的：入库卡片原来那个"居中"动画会跟拖动打架（出现时跳一下），已改成缩放动画
- ⚠️ 改了**前端**（`public/app.js` + `public/style.css`）→ **Ctrl+F5**；这次**没动后端、没动篡改猴脚本**

### ✨ 新增「📄 原文」窗口：先把整篇文本摆出来，再谈分割

- 工作台上多了一个 **「📄 原文」** 按钮，取回分镜时会**自动打开**它 ——
  **顺序改成：先把完整原文贴进这个窗口，然后才分割**，不再是"一取回来就被切碎、看不到全貌"
- **原文不会被覆盖**，三条保证：
  - 分割**只读**窗口里的文本，不回写 → 切分、落条目都不会动到原文
  - **历次原文都留着**（最多 8 份）：左上角下拉能切回任意一次，新的一次不会把旧的挤掉
  - 原文和历史**存在磁盘上** → 刷新页面、关掉重开都还在
- 窗口里显示**层级**：识别到几个**大分镜**、每个大分镜里有几个**小分镜**（用你真实的这次数据：
  3 个大分镜 / 10 个小分镜，分别是 3、4、3 条）
- 能当**独立的切割台**用：直接在窗口里改文本 → 点「✂ 分割成条目」；也可以**自己粘一段文本**进来分割，
  完全不生成也行（适合从别处复制来的分镜）
- 切不开的时候，提示直接写在窗口底部，原文就在眼前 —— 不再弹一个截断 3000 字的旧弹窗
- **层级明确，不会被盖住**：这个窗口的层级（`1090`）**高于提示词工作台（1080）**，
  已经写进 `style.css` 顶部和 `CODE_MAP` 的「3.13 浮层层级」表，以后加浮层只能照表取数字
- ⚠️ 改了**前端** → **Ctrl+F5**；**后端也改了**（要能存原文和历史）→ **重启 node**；
  这次**没动篡改猴脚本**，不用重贴

### ✨ 预设可以存多份了

- 「⚙ 预设」从"只能改一份"变成**预设库**：**＋ 新建** / **⧉ 复制一份** / **✎ 改名** / **🗑 删除**，
  不同题材（Q版打斗 / 15 秒竖屏…）各存一份，用哪个在下拉里选
- **内置默认不可直接改**（想改先"复制一份"）—— 免得出现"改了却存不进去"的糊涂账；
  想还原点「这一份恢复默认内容」
- **你以前改过的那份不会丢**：升级后会自动搬进预设库，命名"我改过的预设"并直接选中

### 🔧 取回复不再靠"数秒"猜 —— 改成看 DeepSeek 自己的「停止」按钮

- 以前"等回复写完"是**数时间**：每 1.5 秒读一次页面，连续 3 次（≈4.5 秒）没变化就当写完了。
  你指出**只有思考链阶段界面会动、正式生成阶段界面不推** —— 这判据两头都不可靠：
  思考链想完停顿一下就"静默"达标（会把推理过程当正式答案取回来），正式内容在写的时候也可能长时间不动
- 现在优先用**页面自己的信号**：DeepSeek 生成时发送按钮会变成**停止**按钮
  - 正在生成 → **绝不取回**（思考链中途停顿不会再被误判成写完）
  - 停止按钮一消失 → **立刻取回**（不再白等 4.5 秒）
  - 万一这个信号读不出来（网站改版），自动退回原来的"静默"判据，**不会卡死取不回来**
- **只等到思考链不再污染条目**：前端把"什么才算正式回答"（有 `###` 或「大分镜N」标题行）一起告诉脚本，
  不像正式回答就继续等；等到超时也**只报错、不落条目**
- **正式生成不推界面 → 脚本替你滚**：等待期间会把最后一条回复滚进视野。
  内容留在视野外时浏览器可能**压根不渲染它**，那就会取回半篇、或者把分隔标记丢掉
- **复制原文改成轮询等结果**：以前点一下死等 600ms，页面慢一点就空手而归、退回不带标记的文本
  （**这次"分割失灵"就是这条路**）。现在拿到就返回、最多点两次
- debug.log 里每次取回都会记下：等到的判据、生成状态、停止按钮找到没、复制按钮、
  `innerText` 与 `textContent` 的长度差、容器位置 —— 下次再出问题，一看就知道断在哪一环
- ⚠️ 这次三样都动了：**篡改猴脚本升到 1.7.2**（重新粘贴覆盖 + 刷新 DeepSeek 页面）、
  **前端 Ctrl+F5**、**后端也改了 → 重启 node**

### 🔧 点「⚙ 预设」好像没反应？其实弹窗一直弹在**工作台背后**

- 真因是**层级搞反了**：弹窗遮罩 `z-index` 是 950，而提示词工作台面板是 1080 ——
  工作台一开着，弹窗就渲染到面板**后面**，看起来就像"点了没反应"
- 现在**弹窗盖在所有面板上面**了，点「⚙ 预设」正常弹出在工作台之上
- 同一个毛病的另外三个受害者一起修了：
  - **提示（toast）**："已替换为 3 条分镜""预设已保存"这类提示原来也在工作台下面，会被盖住
  - **上传进度条**：大工作台居中时正好压住它
  - **点图全屏预览（灯箱）**：开着工作台点素材图会"什么都没出现"，也是这个原因
- 层级顺序已经写死在 `style.css` 顶部和 `CODE_MAP.md` 的「3.13 浮层层级」，
  以后新增浮层只能按那张表取数字，不许随手写

### 🔧 分割又失灵：这次是"整篇被塞成一条"，现在切不开会直接告诉你

- **你点完生成，3 个大分镜被并成 1 条** —— 内容其实完全正确（大分镜1/2/3，各 20 秒，小分镜 3/4/3 条），
  只是**分隔标记在取回来的路上丢了**，而程序**一声不响**地把它当成"只有一个分镜"填了进去
- 现在换了三层保险：
  1. **不再只认 `###`**：预设本来就要求每个大分镜第一行写「大分镜N｜小标题」，
     标记丢了就**按这行标题切** —— 这次的真实文本靠它就能正确切成 3 条
  2. **预设改成 `###` 紧跟标题、中间不留空格**（`###大分镜1｜…` 网页不会当标题渲染，标记就不会被吞掉）
  3. **切不开不再装成功**：如果文本里明明有 2 个以上大分镜、却只切出 1 条，
     会**弹出取回的原文并报错**，不会再把一整篇塞进一条条目里
- 每次取回复都会把页面诊断（复制按钮找到没、页面用什么标签渲染标题、文本里还有没有 `###`）
  记进 `debug.log` —— 下次真出问题，一看就知道是哪一环断的
- ⚠️ 这次改了**篡改猴脚本**（升到 1.7.1）：要在篡改猴里**重新粘贴覆盖**一次并刷新 DeepSeek 页面；
  **改了前端** → 浏览器 **Ctrl+F5**

### 🔧 小分镜数量不再写死「3~5 个」

- 上一版预设写死了"必须由 3~5 个小分镜组成"，太僵。现在改成**数量由内容决定**：
  动作密集就多拆几个，一个连贯的长动作**也可以只用一两个镜头**；
  不要为了凑数量硬拆，**但也不许把整个大分镜塞成一个镜头**（这条仍然写死）
- 实际效果：最近这次生成给的是 3 / 4 / 3 条小分镜，时长区间各自加起来正好 20 秒

## 2026-09-26

### 🔧 预设去歧义：大分镜内部必须是「多个小分镜」，不是长镜头节拍

- 上一版预设里写的"多个小分镜 / 动作节拍"**有歧义** —— DeepSeek 会理解成"一个长镜头内部的节拍"，
  于是每段都写成「单镜头 one-shot 无剪辑」。现在写死了：
  **大分镜内部是「多镜头切换、多机位剪辑」**，必须由 3~5 个小分镜组成（**这条已在 09-26 改掉，见上**），
  每个小分镜写清「镜号 + 时长区间 + 景别/机位/镜头运动 + 画面内容」，时长区间加起来正好等于单次时长
- 补了两条边界规则：**参考资料里的默认时长以你选的为准**（skill 里写 4-15 秒、你选 20 秒就按 20）；
  **本要求与参考资料冲突时，以本要求为准**
- 预设里直接放了一段**结构样例**（照它写），比抽象描述管用得多
- ⚠️ 如果你之前点「⚙ 预设」改过并保存了，新默认不会自动生效 —— 点「**恢复默认**」就能吃到新版

### 🎬 分镜改成「大分镜」口径 + 预设提示词可改

- **"10s / 15s" 是豆包这类视频模型一次能生成的时长选项**，不是每个小镜头的长度。
  现在指令里写清楚了这套分层：**先按剧情排戏 → 按「总时长 ÷ 单次时长」酌情分成若干个大分镜**
  （比如 60 秒 → 4 个 15 秒大分镜），**每个大分镜内部再写多个小分镜 / 动作节拍**；
  脚本截出来的就是这**几个大分镜**，每个正好对应一次豆包生成
- **总时长不用你配**：剧情或 skill 参考资料里写了就按它，没写就让 DeepSeek 自己判断
- 新增「**⚙ 预设**」按钮：点开就能改「生成分镜」投出去的那段指令，
  支持占位符 `{{script}}` 剧情 / `{{seconds}}` 秒数 / `{{split}}` 分隔符，改完记住，另有「恢复默认」

### 🔧 修「分割失败」和「取到上一次内容」（两个独立的问题）

- **分割失败的真因**：AI 按要求用 `###` 分段，但**网页把 `###` 渲染成了标题**，
  脚本抓文本时那三个 `#` 已经不在文本里了 → 自然切不出分镜。现在两手：
  抓 DOM 时把标题**还原回 `###`**；同时**优先点页面上的「复制」按钮拿 Markdown 原文**（更保真），
  抓 DOM 只作兜底
- **取到上一次内容的真因**：取回复时页面还挂着上一条回复，"内容不变就算写完"当场成立
  （日志里两次取回字数完全相同、第二次只花 6 秒）。现在进这一步先记**基线**
  （用发送前的内容），**只有内容和基线不同**才算这一轮的新回复
- 万一还是切不出来：会**直接把取回的原文摊在弹窗里**给你看，不用翻日志

### 🧹 投放助手界面合并（脚本一份通用，界面也该是一份）

- 左侧栏「📤 投放素材助手」**直接开面板**，不再先弹一层"选平台"菜单
- 面板里改成 **一个平台下拉 + 唯一一个「打开」按钮**（原来是三个平台各一个面板、各一个"打开 XXX"）
- 安装状态和 4 步说明**通用一份**（脚本三个站通用）；**装好之后自动折叠**，不占地方
- 选的平台会记住，下次打开还是上次那个

### 🧠 用 DeepSeek 生成分镜（这才是最初要的那条链路）

- 工作台新增「**🧠 生成分镜**」：写好剧情 + 勾好 skill（只列 `.md`/`.txt`，yaml 根本不出现）→
  一键投给 **DeepSeek 网页版**，投完**自动发送**（不用你去 DeepSeek 页面点）
- 指令里写明了「每个镜头 N 秒」（用你在工作台选的档位）和「每段以 `###` 开头」，
  让 AI 的输出格式可控、能被程序切开
- DeepSeek 写完后 webui **自动取回回复**（脚本等它生成完再抓：内容连续 4.5 秒没变化就算写完；
  **会自动跳过"思考过程"**，不会把内心戏当成分镜），按 `###` 切成一条条 → **自动填进分镜条目**，
  不再需要你手工勾选；旁边有「**查看**」（打开预览，改完可重填）和「**撤销**」（回到生成前的条目）
- 取回的长文本**不写进 `debug.log`**（只记多少字），日志不会被撑爆
- **skill 是组合投放的**：勾几个就投几个，脚本逐个挂附件（间隔 0.4 秒保证顺序）；
  上限 20 个，超出会**明确告诉你少了几个**（以前是静默截断）；
  上传控件会自动挑"能收这个文件"的那个；**万一所有 skill 都没投进去，脚本会中止发送**并报错，
  不会发出一条"没有 skill"的消息污染对话

### 🐋 支持 DeepSeek 网页版

- 投放素材助手新增 **DeepSeek**（`chat.deepseek.com`）：脚本、Edge 扩展、左侧菜单三处都加上了
- 工作台标题栏新增「**投放到**」下拉（🫘 豆包 / 🐋 DeepSeek），选谁就投给谁；
  **任务按站点隔离** —— 豆包页面不会把给 DeepSeek 的任务领走
- 选择器**不靠猜**：DeepSeek 用的是公开自动化项目实测过的
  （输入框 `#chat-input`、发送 `button[aria-label="Send message"]`、回复 `.ds-markdown`），
  而且每个平台的输入框/发送按钮都是**分级兜底**，一条失效自动退到下一条
- 加了新平台，选择器来源和"怎么再加一个平台"都记在 `CODE_MAP.md` 2.6
- ⚠️ 更新后照旧：重新覆盖脚本（Ctrl+A 覆盖粘贴）+ 刷新对应站点页面

### 🔧 修「投放时好时坏：找不到输入框 / 发送按钮」

- **根因**：豆包**对话页里嵌着一个云盘 iframe**（`/drive-iframe/drive/home/`），
  而篡改猴会把脚本注入到**所有 frame** —— 那个 iframe 里的实例同样在轮询、同样能抢走任务，
  抢到就必然报"找不到输入框 / 发送按钮"。因为日志里显示的是 iframe 的地址，
  看起来像"跑到云盘页去了"，其实人一直在对话页
- 现在**只在顶层页面干活**（iframe 里的实例直接退出）+ **干不了活的页面不许领任务**（双保险）
- 失败信息里会写明**当前页面路径**（例如"当前豆包页面（/drive-iframe/drive/home/）不是对话/生成页"），
  一眼看出是不是页面不对；入库的日志也会记下任务被哪个页面领走
- **提示词没投进输入框现在算失败**（以前图投进去了、提示词没进去，却显示"已投 + 提示词"，
  你一点发送就发出个没提示词的内容）
- 顺手治了两个日志噪音：脚本 1.2 秒一次的轮询请求不再写进 `debug.log`；
  工作台的 skill 树从"每次重绘都拉一次"改成"只拉一次"
- ⚠️ 用法提醒：投放时让豆包停在**对话 / 生成页**；同时开多个对话页时任务可能投到任意一个能用的页面

### 🔧 修「找不到发送按钮」

- 查了开源的豆包自动化实现（[doubao-playwright-skill](https://github.com/zynieie/doubao-playwright-skill)）：
  **豆包网页版是按 `Enter` 发送的**，代码里根本不点发送按钮。所以「发送」现在的顺序是：
  先找按钮并点击 → **找不到或不可用就退回「在输入框按 Enter」**
- 输入框定位优先认豆包的 `textarea.semi-input-textarea`（Semi 设计系统），其它站点再退回通用选择器
- 万一还是失败：脚本会**自动把页面上的按钮清单（标签 / class / aria / 坐标）回报回来**，
  写进 `debug.log` —— 我按真实 DOM 适配，不用你手抄

### 📤 投放通道（工作台 → 豆包，一键送图 + 提示词 + 点发送）

- 工作台每条分镜多了 **📤 投放**：助手脚本会把这条的**多张图一张一张**投进豆包输入框
  （**间隔 0.5 秒**，确保人物和图片不串位），再把这句提示词投进去
- 投完条目状态变成「**✅ 已投放，待发送**」，旁边出现 **发送** 按钮 —— 你确认没问题再点，
  脚本去点豆包的发送按钮（**找不到按钮就报错，绝不猜坐标瞎点**）
- 状态全程可见：排队中… / 正在投图… / 已投放待发送 / 已发送 / ❌ 失败原因
- 脚本崩了或页面关了也**不会卡死**：任务 90 秒无人回执会自动退回重来
- 前提：豆包页面开着、助手脚本已启用（工作台入队后 4 秒没人领，会提醒你去检查）

### 🖼 一条提示词可以配多张图

- 工作台的每条分镜现在支持**多张参考图**（人物 + 场景 + 风格…），拖进来是**追加**、不是覆盖
- 同一张图不会重复添加；每条**最多 12 张**（防止手滑拖进来一大堆）
- 鼠标移到缩略图上出现 **×** 可以单删；条目上的 🖼 按钮清空这一条的所有配图
- 老数据（只有单张图）会自动升级成数组，不用重新配

### ✍️ 提示词工作台（第一期：编排）

- 左侧栏底部新增「**✍️ 提示词工作台**」：写剧情 + 树形勾选 skill + 选镜头秒数（5/10/15/20s）
- **放大态 ↔ 缩小态**：放大是居中大窗，**按 Esc 缩成右下角小框**继续用
- **缩小后仍能从素材管理器拖图片进来** —— 拖到哪一条就配给哪一条（只配图，不移动文件）；
  拖到空白处自动填第一条没配图的
- 分镜条目可增删、可上下移、可单独改提示词；**存在数据库里，刷新/重启都不丢**
- 这一步只做"编排"；自动投放（送图 + 提示词给豆包、点发送）在下一次更新

### 📚 技能目录（提示词模板）—— 提示词投放的第一步

- 左侧栏新增「**技能（提示词模板）**」分区：可以在磁盘**任意位置**挂载放提示词模板的目录
  （有人 skill 和漫剧目录不在一起，所以做成"挂载"而不是写死路径）
- **挂一次就记住**（存在 `data.db`），想换路径随时移除重挂
- 只认 `.md` / `.txt` / `.markdown`：**yaml / json 这类不列也不投**（AI 网页读不好）
- 点文件名可预览内容；目录被移走、盘符变了会标「⚠️ 目录不存在」
- 与素材根目录**完全隔离**：不进素材树、收件箱不监听、回收站不会在里面建东西

### 🚀 新增启动器（可以选浏览器了）

- 双击 **`启动.bat`** 先出一个菜单：**用哪个浏览器打开 / 是否自动打开页面 / 端口** ——
  **选一次就记住**，以后双击直接回车
- **不再写死浏览器安装路径**：自动扫描注册表 `StartMenuInternet`、`App Paths`，再退到常见路径和 `PATH`。
  装在 D 盘、绿色版、公司定制浏览器（如 Lenovo SLBrowser）都能发现；实在扫不到的，
  菜单里有「**手动输入**」可以粘贴 exe 路径
- 网页 **⚙ 设置**里的浏览器下拉也换成了这份**真实扫描结果**
- **加新的启动选项不用改代码结构**：在 `launcher.js` 顶部的选项表里加一条，
  菜单、记忆、参数传递、"手动输入"入口都会自动带上（见 `CODE_MAP.md` 2.4）
- 原先的 `启动-Chrome.bat` 已删掉 —— 启动器里选一次 Chrome 就够了

### 🆕 新文件自动入库（收件箱）

- Edge **下载完的文件一落进素材夹，就自动弹卡片问「进哪个库」**，可以顺手改名再入库
- **从资源管理器拖进网页的文件同样会问**（以前传完就结束了，什么也不问）
- **收哪个目录不用配**：自动跟随 Edge 的下载设置，你在 Edge 里改了它就跟着变；
  设置面板里只做只读展示 + 「重新读取 Edge 设置」
- 入库位置按**目录层级**展示（根目录 / 各级文件夹 / 虚拟分类），缩进成树，不再是平铺一长条
- **⭐ 最近常用**：入库过两次以上的位置自动置顶（标「用过 N 次」）；
  连续 10 次入库没用它就自动消失（"直接进未归类"不算在这 10 次里）
- 改名框**后缀固定不变**：改名字不会把 `.png` / `.mp4` 弄丢；重名不覆盖，自动变 `xxx (1)`
- 三种策略真的生效了：**智能**（文件名乱才问）/ **总是询问** / **全自动**（不打扰，进上次的位置）
- 首次启动不会把文件夹里已有的文件当成新文件（不会一开就弹一堆）

### 🐛 修复

- 从资源管理器拖文件进网页，**不再被浏览器抢去开新标签页**（上传终于能成功）
- **网格和列表里所有文件都能拖了**：以前只有图片拖得动，视频、文件夹、没有缩略图的文件都拖不动
  （原因是缺失 `draggable` 声明）
- 网页自己的操作（上传 / 改名 / 移动 / 恢复）**不会再弹入库卡片给自己**
- 拖拽遮罩不会再卡在屏幕上不收

### ✨ 改进

- 入库卡片移到**屏幕正中**并加背景遮罩，不容易看不见
- 设置里「新下载文件的处理策略」从摆设变成真的生效，另加「监听根目录里的新文件」开关
- 拖拽判据收敛到一处：网页内部拖拽（移动素材）和外部拖入（上传）不会再互相串

### 📤 投放素材助手 v1.6.0

- **新增支持 Pavo**（`app.pavo-ai.work`）：**同一份脚本、同一个面板**，豆包和 Pavo 都能投放；
  左侧栏「📤 投放素材助手」里多了一个 🎬 **Pavo** 入口，面板标题会显示当前在哪个站
- **抓标题栏可以拖动整个面板** —— 挡住提示词就拖走，拖到底部就只占下半屏
- 标题栏 **`－` / `＋`** 调预览图大小（90~420px），**`⌂`** 一键复位
- **位置和大小会记住**：关掉豆包再打开，面板还在你放的地方
- 修掉一个隐患：拖动时指针划过页面里的 iframe 不再把拖动"甩掉"
- 三份脚本同步更新（篡改猴脚本 / 网页"复制脚本代码"用那份 / Edge 扩展）
- ⚠️ 更新后需要**重新覆盖脚本**（篡改猴里 Ctrl+A 全选粘贴新代码并保存，或扩展点"重新加载"），
  然后刷新豆包页面

### 🔧 开发相关

- 代码地图拆成两份：手写的 `CODE_MAP.md`（不再含行号）+ 自动生成的 `CODE_MAP.index.md`；
  改完代码跑 `node map.js` 刷新，查行号用 `node map.js <关键词>`

## 2026-09-21 · 初始版本

- 零依赖本地 WebUI（`node server.js`，默认 http://127.0.0.1:8899）
- 直接铺满图片/视频预览：懒加载 + IndexedDB 缩略图缓存、视频首帧与时长、悬浮自动播放、全屏灯箱
- 整理素材：拖拽移动、批量改名（前缀+序号）、新建项目模板目录、重复文件检测、回收站
- 虚拟分类（只记文件名引用，不动文件）：归类 / 移出归类 / 转为实体文件夹（平移或复制）
- 「散-未归类」虚拟节点、全选整个视图（含未加载）、右键菜单、操作日志
- 投放素材助手：豆包侧边栏，点一下把素材送进豆包输入框
- 安全：只绑 127.0.0.1、所有路径锁死在已添加的根目录内、删除即进回收站、跨域只放行 doubao.com
