# 项目须知：漫剧素材管理器

> 这个文件是给 AI 助手看的（`AGENTS.md` 约定）。人看的说明在 `README.md`。

## 改代码前

1. 先读 **`CODE_MAP.md`** —— 文件职责、模块规则、8 条踩坑、故障排查表、配置单一数据源。
2. 改动面大就先 `git commit`（`git log --oneline` 看历史），别在无版本控制下做渐进优化。

## 改代码后（**别忘**）

```powershell
node map.js          # 刷新 CODE_MAP.index.md（行号索引，自动生成）
node map.js 收件箱    # 查行号：按关键词直接定位
node map.js --print  # 只看不落盘
```

- **禁止手工维护行号。** `CODE_MAP.md` 里一个行号都没有 —— 这是故意的：
  行号每改一次代码就整体漂移，手工重刷几十处纯属搬砖。行号一律由 `map.js` 生成到
  `CODE_MAP.index.md`（那份别手改）。

## 临时文件规矩 —— **别丢在项目根目录**

验证脚本、试跑产物、一次性小工具，一律写到 **`.git/_xxx.js`** 里跑，跑完在同一条命令里删掉。

- **绝对不要**往项目根目录丢 `_xxx.js`：用户会**一键 `git add -A` 推到公开仓库**，
  忘了删就直接上墙（真栽过：验证脚本的写入卡片出现在用户的改动列表里）
- 写进 `.git/` 是**从根上没有风险**：那是 git 内部目录，永远不会被提交
- 兜底（**别依赖**）：`.gitignore` 已加 `_*.js` / `_*.bat` / `_*.cmd`；
  `scrub.js` 也会把"待提交里的下划线文件"报出来

## 批处理脚本（.bat）规矩

`.bat` 一律只当 **纯 ASCII 启动器**，真正的逻辑放旁边的 `.ps1` 里（`.ps1` 存 **UTF-8 带 BOM**）。
下面两条都是真踩过的坑：

- **`.bat` 里不要写中文、也不要切 `chcp 65001`** —— cmd 是按系统代码页（GBK）**逐行解析**批处理文件的，
  中文会被解成乱码，乱码字节里混进 `&` `|` `(` 就把命令拆碎，满屏"不是内部或外部命令"
  （表现：中文的 .bat 双击后一片乱码；跟放在哪个盘无关）
- **`.bat` 里不要用 `( )` 括号块** —— 块内一个没转义的 `)` 会提前结束块，把 `pause` 之类漏到块外执行
  （症状：双击后永远停在"请按任意键继续"）。启动器用 `goto` 分流，不用括号块

改完 `.bat` / `.ps1` 跑一次 `node scrub.js`：它会报"含非 ASCII 又切 65001"这种坏组合。
**新写任何给用户双击的脚本，先照这两条来，别再让用户当测试机。**

## 交付给用户时

- 改了 **前端**（`public/`）→ 提醒 **Ctrl+F5**（浏览器 JS 是页面加载那一刻装进去的）。
- 改了 **后端**（`server.js` / `db.js`）→ 提醒 **重启 node**（没有热更新）。
- 两件都改了都要提醒；但**同一件事不要反复要求用户做**。

## 硬规矩（细节见 `CODE_MAP.md` 踩坑记录）

- 所有路径必须过 `resolveSafe()`（安全命脉）。
- "复制"有两个东西：系统剪贴板 `copyToClipboard()` vs 网页内部 `S.internalClip`，
  文案必须写清是哪一个。
- 新文件的**两个入口**（后台监听 / 拖进网页上传）都必须走 `dispatchNewFile()`。
- 任何"往根目录顶层写文件"的接口都必须调 `markSelfWrite()`。
- 拖拽判据只有 `dragKind()` 一处；`dragover`/`drop` **必须** `preventDefault()`。
- 拆 `bind*` 函数后必须数调用点（跑 CODE_MAP 踩坑① 里那条 `Select-String`）。

## 启动 / 自测

- 启动：`node server.js`（默认 `http://127.0.0.1:8899`），或双击 `启动.bat`。
- **自测不要碰用户的 `data.db`**：把 `server.js` / `db.js` / `public` 复制到临时目录（如 `_e2e/`），
  换端口跑（`node server.js --port 8897`），测完删掉那个目录。

## 提交信息（commit message）规范

`commit message` 会**永久留在 git 历史里**，还**直接显示在 GitHub 的文件列表和 commits 页上** ——
它比文件内容更显眼，而且改它要重写历史 + `force push`（不可逆）。这个仓库是**公开**的，所以：

- **不许**把需求方的原话抄进去（转述式引用：某某说"…"、某某原话"…"、某某回"…"）
- **不许**出现真实姓名 / 邮箱 / 本机绝对路径 / 具体作品名与项目名
- 只写**中性事实**：做了什么、为什么。例：`fix: 取回复改看页面「停止生成」信号`
- **正文（body）同样受管** —— 那里最容易顺手写"某某反馈说…"，一句都不要写
- 也别在信息里谈论仓库自身的整理/清理话题（这类内容由 `scrub.js` 自动检测，不用写进规范里）

推送前跑一遍：`node scrub.js`（会扫**最近 30 条提交信息** + 所有要公开的文件）。
**光改当前文件没用 —— message 要改就得重写历史。**
